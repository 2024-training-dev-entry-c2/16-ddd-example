dependencies {
    implementation 'io.projectreactor:reactor-test:3.6.8'
    implementation(project(':shared:application'))
    implementation(project(':shared:domain'))
    implementation(project(':industries:domain'))

}